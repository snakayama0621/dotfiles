# Yazi

高速なターミナルファイルマネージャー。

## 起動方法

```bash
# yaziを起動
yazi

# 終了時にそのディレクトリに移動（.zshrcで定義）
yy
```

## キーバインド

### 基本操作

| キー | 動作 |
|------|------|
| `j/k` | 上下移動 |
| `h/l` | 親ディレクトリ/ディレクトリに入る |
| `Enter` | ファイルを開く |
| `q` | 終了 |
| `.` | 隠しファイル表示切替 |

### 検索

| キー | 動作 |
|------|------|
| `/` | ファイル名をフィルタ（現在のディレクトリ内） |
| `S` | ファイル内容検索（ripgrep） |
| `z` | zoxideでディレクトリジャンプ |
| `Z` | fzfでファイル検索 |

### ソート

| キー | 動作 |
|------|------|
| `sn` | 名前でソート |
| `sN` | 名前でソート（逆順） |
| `ss` | サイズでソート |
| `sS` | サイズでソート（逆順） |
| `sm` | 更新日時でソート |
| `sM` | 更新日時でソート（逆順） |

### クイックジャンプ（カスタム）

| キー | 動作 |
|------|------|
| `gd` | ~/dotfilesに移動 |
| `gh` | ホームに移動 |
| `gc` | ~/.configに移動 |
| `gD` | ~/Downloadsに移動 |
| `gw` | ~/workに移動 |
| `g/` | ルートディレクトリに移動 |
| `go` | Obsidian vaultに移動 |

### ファイル操作

| キー | 動作 |
|------|------|
| `Ctrl+n` | 新規ファイル/ディレクトリ作成 |
| `Ctrl+r` | 拡張子の前にカーソルを置いてリネーム |
| `Space` | 選択してカーソル移動 |
| `V` | ビジュアルモードに入る |
| `y` | コピー |
| `p` | ペースト |
| `d` | 削除 |
| `r` | リネーム |
| `!` | 確認付きでシェルコマンドを実行 |

## 設定ファイル

- `yazi.toml` - メイン設定（表示オプション等）
- `keymap.toml` - キーバインド設定

## 依存パッケージ（プレビュー機能用）

- `ffmpegthumbnailer` - 動画サムネイル
- `unar` - アーカイブプレビュー
- `poppler` - PDFプレビュー
- `imagemagick` - 画像処理
